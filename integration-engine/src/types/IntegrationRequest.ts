/**
 * A Interface that contains all the properties that an IntegrationRequest should have.
 */
export interface IntegrationRequest {
    department: string;
    operation: string;
    citizenId: string;
}